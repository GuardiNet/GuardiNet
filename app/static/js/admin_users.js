// Placeholder pour une future intégration avec la base de données
        // Les utilisateurs seront chargés dynamiquement
        document.getElementById('search').addEventListener('input', function(e) {
            // Logique de recherche
        });

        document.getElementById('role-filter').addEventListener('change', function(e) {
            // Logique de filtrage par rôle
        });